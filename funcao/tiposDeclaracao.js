console.log(soma(3, 4));

// function declaration (declarada no início)
function soma(x, y) {
    return x + y
}

// function expression
const sub =  function (x, y) {
    return x - y
}

// named function expression (pouco usada)
const mult = function mult(x, y) {
    return x * y
}